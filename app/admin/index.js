const { getSeasonData } = require('./get-season-data')
const { getPrizes } = require('./get-prizes')
const { getFees } = require('./get-fees')


module.exports = {
  getSeasonData,
  getPrizes,
  getFees
}
